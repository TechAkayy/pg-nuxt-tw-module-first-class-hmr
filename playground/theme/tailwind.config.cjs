const colors = require("tailwindcss/colors");

module.exports = {
  theme: {
    extend: {
      colors: {
        brand: colors.red["500"],
        accent: colors.fuchsia["500"],
      },
    },
  },
};
